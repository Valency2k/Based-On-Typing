{
    "name": "based-on-typing",
    "version": "1.0.0",
    "description": "Blockchain-enhanced typing game on Base network",
    "scripts": {
        "dev": "live-server --port=3000 --host=localhost",
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy": "npx hardhat run scripts/deploy.js --network base"
    },
    "keywords": [
        "blockchain",
        "typing-game",
        "base",
        "ethereum",
        "web3"
    ],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.0",
        "live-server": "^1.2.2"
    },
    "dependencies": {
        "@chainlink/contracts": "^1.5.0",
        "@farcaster/miniapp-sdk": "^0.2.1",
        "@farcaster/miniapp-wagmi-connector": "^1.1.0",
        "@openzeppelin/contracts": "^5.4.0",
        "dotenv": "^17.2.3",
        "viem": "^2.39.3",
        "wagmi": "^3.0.1"
    }
}
